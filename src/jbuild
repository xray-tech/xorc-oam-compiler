(library
 ((name orcml)
  (libraries (base menhirLib stdio js_of_ocaml js_of_ocaml.weak))
  (preprocess (pps (ppx_jane)))
))

(menhir
 ((flags (--explain --dump))
  (modules (parser))))

(jbuild_version 1)
